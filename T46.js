var n="0230.000";
console.log(Zero(n))
function Zero (n) {
    var n=Number(n)
    var numstr=n.toString()
    console.log(numstr)
    // n = n.replace(/^0+/, '',/^0+/);
    // for (let i = 0; i < n.length; i++) {
    //     console.log(n[i])
    //     // if(a[])
        
    // }
    // console.log(n)
    
}